<h2>New user</h2>
<div id="new_user" class="expandable_form">
	<%= error_messages_for :user %>
	<% form_for :user, :url => users_path do |f| -%>
	<%= render :partial => 'form', :locals => {:f => f} -%>
	or <%= link_to "Cancel", root_url %>
	<% end -%>
</div>
