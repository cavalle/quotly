<h2><span><%= @presenter[:nickname] %>'s saved quotes</span></h2>
<div class="quotes">
  <%= render :partial => 'quotes/quote', :collection => @presenter[:quotes] %>
<% if @presenter[:quotes].empty? %>
  <div class="notice">
    No quotes saved by <%= @presenter[:nickname] %> yet
  </div>
<% end %>
</div>
